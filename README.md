# 📘 Fund Fee Only

ระบบจัดการเอกสาร PDF และสกัดข้อมูลใบกำกับภาษีกองทุนแบบครบวงจร

## 📋 เกี่ยวกับโปรแกรม

Fund Fee Only เป็นโปรแกรมที่ออกแบบมาเพื่อช่วยในการจัดการไฟล์ PDF และสกัดข้อมูลจากใบกำกับภาษีกองทุนต่างๆ โดยอัตโนมัติ โปรแกรมรองรับการทำงานกับหลายกองทุนและสามารถส่งออกข้อมูลเป็นไฟล์ Excel ได้ทันที

## ✨ ฟีเจอร์หลัก

### 📄 Merge PDF
- รวมไฟล์ PDF หลายไฟล์ในโฟลเดอร์ให้เป็นไฟล์เดียว
- รองรับไฟล์ PDF ที่มีรหัสผ่าน
- ประมวลผลแบบอัตโนมัติ

### 📊 DAOL Extractor
- สกัดข้อมูลใบกำกับภาษี DAOL จากไฟล์ PDF
- ส่งออกข้อมูลเป็นไฟล์ Excel
- รองรับการประมวลผลหลายไฟล์พร้อมกัน

### 🏦 SCBAM Extractor
- ดึงข้อมูลจาก Statement ของ SCB Asset Management
- รองรับไฟล์ PDF หลายหน้าในไฟล์เดียว
- สกัดข้อมูล: เลขที่, วันที่, Unitholder No., ชื่อกองทุน, Fee, VAT, total fee

### 📋 Eastspring Extractor
- แปลงข้อมูลใบกำกับภาษี Eastspring จาก PDF → Excel
- รองรับการประมวลผลหลายไฟล์ในโฟลเดอร์
- มีระบบ OCR สำหรับไฟล์ PDF ที่เป็นภาพ

### 💼 Asset Fund Extractor
- สกัดข้อมูลใบกำกับภาษี Asset Fund จาก PDF
- แสดง Raw Text และตารางข้อมูลใน Console
- ส่งออกข้อมูลเป็นไฟล์ Excel

## 🛠️ ข้อกำหนดของระบบ

### Python Version
- Python 3.8 หรือสูงกว่า

### Dependencies
```
ttkbootstrap
pdfplumber
openpyxl
PyPDF2
pytesseract
Pillow
```

### ระบบปฏิบัติการ
- macOS
- Windows
- Linux

### OCR Engine (สำหรับไฟล์ PDF ที่เป็นภาพ)
- Tesseract OCR
  - macOS: `brew install tesseract`
  - Windows: ดาวน์โหลดจาก [Tesseract OCR](https://github.com/UB-Mannheim/tesseract/wiki)
  - Linux: `sudo apt-get install tesseract-ocr`

## 📦 การติดตั้ง

### 1. Clone Repository
```bash
git clone <repository-url>
cd fundfeeonly
```

### 2. ติดตั้ง Dependencies
```bash
pip install -r requirements.txt
```

หรือติดตั้งทีละตัว:
```bash
pip install ttkbootstrap pdfplumber openpyxl PyPDF2 pytesseract Pillow
```

### 3. ติดตั้ง Tesseract OCR (ถ้าต้องการใช้ OCR)
- **macOS**: `brew install tesseract tesseract-lang`
- **Windows**: ดาวน์โหลดและติดตั้งจาก [Tesseract OCR](https://github.com/UB-Mannheim/tesseract/wiki)
- **Linux**: `sudo apt-get install tesseract-ocr tesseract-ocr-tha`

## 🚀 วิธีการใช้งาน

### เริ่มต้นโปรแกรม
```bash
python main.py
```

### การใช้งานแต่ละฟีเจอร์

#### 1. Merge PDF
1. เลือกเมนู "Merge PDF"
2. เลือกโฟลเดอร์ที่มีไฟล์ PDF ที่ต้องการรวม
3. ใส่รหัสผ่าน (ถ้าไฟล์ PDF มีรหัสผ่าน)
4. คลิก "รวมไฟล์ PDF"
5. ไฟล์ที่รวมแล้วจะถูกสร้างในโฟลเดอร์เดียวกัน

#### 2. DAOL Extractor
1. เลือกเมนู "DAOL Extractor"
2. เลือกโฟลเดอร์ที่มีไฟล์ PDF ของ DAOL
3. ใส่รหัสผ่าน (ถ้ามี)
4. คลิก "เริ่มประมวลผล"
5. ไฟล์ Excel จะถูกสร้างในโฟลเดอร์เดียวกัน

#### 3. SCBAM Extractor
1. เลือกเมนู "SCBAM"
2. เลือกไฟล์ PDF ของ SCB Asset Management
3. ใส่รหัสผ่าน (ถ้ามี)
4. คลิก "เริ่มประมวลผล"
5. ไฟล์ Excel จะถูกสร้างในโฟลเดอร์เดียวกับไฟล์ PDF

#### 4. Eastspring Extractor
1. เลือกเมนู "Eastspring"
2. เลือกโฟลเดอร์ที่มีไฟล์ PDF ของ Eastspring
3. ใส่รหัสผ่าน (ถ้ามี)
4. คลิก "เริ่มประมวลผล"
5. ไฟล์ Excel จะถูกสร้างในโฟลเดอร์เดียวกัน

#### 5. Asset Fund Extractor
1. เลือกเมนู "Asset Fund"
2. เลือกโฟลเดอร์ที่มีไฟล์ PDF ของ Asset Fund
3. ใส่รหัสผ่าน (ถ้ามี)
4. คลิก "เริ่มประมวลผล"
5. ดูข้อมูลใน Console และไฟล์ Excel จะถูกสร้างในโฟลเดอร์เดียวกัน

## 📊 ข้อมูลที่สกัดได้

โปรแกรมจะสกัดข้อมูลต่อไปนี้จากใบกำกับภาษี:

- **ลำดับ** - ลำดับของรายการ
- **เลขที่** - เลขที่ใบกำกับภาษี
- **วันที่** - วันที่ออกใบกำกับภาษี
- **Unitholder No.** - เลขบัญชีผู้ถือหน่วยลงทุน
- **ชื่อกองทุน** - ชื่อกองทุน
- **Fee** - ค่าธรรมเนียมไม่รวมภาษีมูลค่าเพิ่ม
- **VAT** - ภาษีมูลค่าเพิ่ม
- **total fee** - ค่าธรรมเนียมรวมภาษีมูลค่าเพิ่ม

## 📁 โครงสร้างไฟล์

```
fundfeeonly/
├── main.py              # ไฟล์หลักของโปรแกรม
├── mergepdf.py          # ฟีเจอร์ Merge PDF
├── doal.py              # DAOL Extractor
├── scbam.py             # SCBAM Extractor
├── eastspring.py        # Eastspring Extractor
├── assetfund.py         # Asset Fund Extractor
└── README.md            # เอกสารนี้
```

## 🔧 การแก้ไขปัญหา

### ปัญหา: ไม่สามารถอ่านไฟล์ PDF ได้
- ตรวจสอบว่าไฟล์ PDF ไม่เสียหาย
- ตรวจสอบรหัสผ่านว่าถูกต้อง
- ลองเปิดไฟล์ PDF ด้วยโปรแกรมอื่นก่อน

### ปัญหา: OCR ไม่ทำงาน
- ตรวจสอบว่าได้ติดตั้ง Tesseract OCR แล้ว
- ตรวจสอบ path ของ Tesseract ในโค้ด
- สำหรับ macOS: ตรวจสอบว่า path เป็น `/opt/homebrew/bin/tesseract` หรือ `/usr/local/bin/tesseract`

### ปัญหา: ไม่พบข้อมูลที่ต้องการ
- ตรวจสอบว่าไฟล์ PDF มีรูปแบบที่โปรแกรมรองรับ
- ดู Raw Text ใน Console เพื่อตรวจสอบข้อมูล
- ลองใช้ OCR ถ้าไฟล์เป็นภาพ

## 📝 หมายเหตุ

- โปรแกรมจะสร้างไฟล์ Excel ในโฟลเดอร์เดียวกับไฟล์ PDF ที่ประมวลผล
- ชื่อไฟล์ Excel จะเป็น: `TaxInvoice[ชื่อกองทุน].xlsx`
- สำหรับ Asset Fund: ไฟล์จะชื่อ `TaxInvoiceAssetFund.xlsx`
- โปรแกรมจะแสดง Raw Text และตารางข้อมูลใน Console สำหรับการตรวจสอบ

## 👤 ผู้พัฒนา

**NongAumzaap** - 2025

## 📄 License

โปรแกรมนี้พัฒนาสำหรับใช้งานภายในเท่านั้น

## 🔄 อัปเดตล่าสุด

- รองรับ Asset Fund Extractor
- ปรับปรุง UI ให้ใช้งานง่ายขึ้น
- เพิ่มการแสดง Raw Text ใน Console
- รองรับการสกัดข้อมูลจากหลายรูปแบบของใบกำกับภาษี

---

**หมายเหตุ**: หากพบปัญหาหรือต้องการความช่วยเหลือ กรุณาติดต่อผู้พัฒนา
